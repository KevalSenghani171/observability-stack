format_version: 10
pipelines:
  Grafana-deploy:
    group: Grafana_Prometheus
    label_template: ${COUNT}
    lock_behavior: none
    display_order: -1

    materials:
      git-670ccc0:
        git: https://github.com/KevalSenghani171/observability-stack.git
        branch: main
        username: KevalSenghani171
        encrypted_password: AES:OSPBYRsIyHG+WhK7L5dv1A==:JFrp9Sg9BQthWwTKmgPI4s9fAl0XBsV6vbRcNkz5JDIzzL4zCc3j261RZpGS3npe
        shallow_clone: false
        auto_update: true

    stages:
      - Code_Deploy:
          fetch_materials: true
          keep_artifacts: false
          clean_workspace: false

          environment_variables:
            GIT_USER: KevalSenghani171
            NAMESPACE: devops-tools
            TMPDIR: /data/tmp
            BRANCH_NAME: prod

          approval:
            type: manual
            allow_only_on_success: false

          jobs:
            grafana_deploy:
              timeout: 0
              elastic_profile_id: docker-dind
              tasks:
                - exec:
                    command: /bin/bash
                    arguments:
                      - -c
                      - |
                        ./Scripts/grafana_deploy.sh
                    run_if: passed